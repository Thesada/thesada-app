# Quickstart broker: plaintext, internal compose network only.
# Production: TLS + per-device mTLS + ACLs. Do NOT expose to untrusted networks.
listener 1883
allow_anonymous true
persistence true
persistence_location /mosquitto/data/
